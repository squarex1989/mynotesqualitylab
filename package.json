{
  "name": "readroom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "NODE_ENV=development node --env-file-if-exists=.env server.js",
    "build": "next build",
    "start": "NODE_ENV=production node --env-file-if-exists=.env server.js",
    "lint": "next lint",
    "check:metrics": "node scripts/check-metrics.mjs",
    "check:audio": "node --import ./scripts/lib/ts-register.mjs scripts/check-audio-unlock.mjs",
    "check:judge": "node --env-file-if-exists=.env scripts/check-judge.mjs",
    "check": "npm run check:metrics && npm run check:uer && npm run check:audio && npm run check:presence && npm run check:compare-access && npm run check:device-prune",
    "check:presence": "node scripts/check-presence.mjs",
    "check:uer": "node scripts/check-uer.mjs",
    "check:compare-access": "node scripts/check-compare-access.mjs",
    "check:device-prune": "node scripts/check-device-prune.mjs"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.3",
    "express": "^4.21.2",
    "music-metadata": "^11.15.0",
    "next": "^15.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "typescript": "^5.7.3"
  }
}
